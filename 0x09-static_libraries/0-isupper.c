#include "main.h"
/**
 * _isupper - checking uppercase
 * @c: parameter of isupper
 *
 * Return: 1 or 0 if uppercase or lowercase
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
