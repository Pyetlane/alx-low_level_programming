#include "main.h"
/**
 * _abs - absolute value of integer
 * @i: parameter of _abs
 * Description: provides absolute value of an integer
 * Return: integer value
 */
int _abs(int i)
{
	if (i >= 0)
	{
	return (i);
	}
	else
	{
	return (i * -1);
	}
}
