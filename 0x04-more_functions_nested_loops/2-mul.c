#include "main.h"
/**
 * mul - multiple two integers
 * @a: firsdt parameter
 * @b: second parameter
 *
 * Return: multiple of two integere
 */
int mul(int a, int b)
{
	int multiple;

	multiple = a * b;
	return (multiple);
}
