#include "main.h"
/**
 * _isdigit - digit checking
 * @c: parameter of_isdigit
 *
 * Return: 1 or 0 if digit or not
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
