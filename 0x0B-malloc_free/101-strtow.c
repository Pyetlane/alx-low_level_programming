#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * strtow - splits astring into words
 * @str: string input
 *
 * Return: pointer of string of words
 */
char **strtow(char *str)
{
	if (str == NULL)
	{
		return (NULL);
	}
	return (str);
}
